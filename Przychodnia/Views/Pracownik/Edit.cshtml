@model Przychodnia.Models.PracownikViewModel

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Author</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.ID_PRACOWNIK)

        <div class="form-group">
            @Html.LabelFor(model => model.IMIE, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.IMIE)
                @Html.ValidationMessageFor(model => model.IMIE)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.NAZWISKO, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.NAZWISKO)
                @Html.ValidationMessageFor(model => model.NAZWISKO)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.ListaOddzialPracownicy, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @for (int i = 0; i < Model.ListaOddzialPracownicy.Count(); i++)
                {
                    @Html.EditorFor(m => Model.ListaOddzialPracownicy[i].Checked)
                    @Html.DisplayFor(m => Model.ListaOddzialPracownicy[i].Name)

                    @Html.HiddenFor(m => Model.ListaOddzialPracownicy[i].Name)
                    @Html.HiddenFor(m => Model.ListaOddzialPracownicy[i].ID)
                }
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.ListaPracownicySpecjalizacje, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @for (int i = 0; i < Model.ListaPracownicySpecjalizacje.Count(); i++)
                {
                    @Html.EditorFor(m => Model.ListaPracownicySpecjalizacje[i].Checked)
                    @Html.DisplayFor(m => Model.ListaPracownicySpecjalizacje[i].Name)

                    @Html.HiddenFor(m => Model.ListaPracownicySpecjalizacje[i].Name)
                    @Html.HiddenFor(m => Model.ListaPracownicySpecjalizacje[i].ID)
                }
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
