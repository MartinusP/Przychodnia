@using Przychodnia.App_GlobalResources;
@model IEnumerable<Przychodnia.Models.SPECJALIZACJA>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink(MultiLanguage.CreateNew, "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NAZWA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NAZWA)
        </td>
        <td>
            @Html.ActionLink(MultiLanguage.Edit, "Edit", new { id=item.ID_SPECJALIZACJA }) |
            @Html.ActionLink(MultiLanguage.Details, "Details", new { id=item.ID_SPECJALIZACJA }) |
            @Html.ActionLink(MultiLanguage.Delete, "Delete", new { id=item.ID_SPECJALIZACJA })
        </td>
    </tr>
}

</table>
