@using Przychodnia.App_GlobalResources;
@model IEnumerable<Przychodnia.Models.RECEPTA>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink(MultiLanguage.CreateNew, "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DATA_WYKORZYSTANIA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NAZWA_LEKU)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UWAGI)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DATA_WYKORZYSTANIA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NAZWA_LEKU)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UWAGI)
        </td>
        <td>
            @Html.ActionLink(MultiLanguage.Edit, "Edit", new { id=item.ID_RECEPTA }) |
            @Html.ActionLink(MultiLanguage.Details, "Details", new { id=item.ID_RECEPTA }) |
            @Html.ActionLink(MultiLanguage.Delete, "Delete", new { id=item.ID_RECEPTA })
        </td>
    </tr>
}

</table>
