@using Przychodnia.App_GlobalResources;
@model IEnumerable<Przychodnia.Models.ODDZIAL_PRACOWNIK>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink(MultiLanguage.CreateNew, "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ODDZIAL.NAZWA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PRACOWNIK.IMIE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ODDZIAL.NAZWA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PRACOWNIK.IMIE)
        </td>
        <td>
            @Html.ActionLink(MultiLanguage.Edit, "Edit", new { id=item.ID_ODDZIAL_PRACOWNIK }) |
            @Html.ActionLink(MultiLanguage.Details, "Details", new { id=item.ID_ODDZIAL_PRACOWNIK }) |
            @Html.ActionLink(MultiLanguage.Delete, "Delete", new { id=item.ID_ODDZIAL_PRACOWNIK })
        </td>
    </tr>
}

</table>
